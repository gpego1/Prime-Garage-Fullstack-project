info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/auth/login
  body:
    type: json
    data: |
      {
        "username": "user@email.com",
        "password": "user123"
      }
  auth:
    type: basic
    username: user
    password: e79e6e20-3d26-4c70-afba-3e48c4704aad

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
